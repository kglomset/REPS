package com.reps.enums;

public enum MuscleRole {
    /** 1 set counts as 1 set towards this muscle group volume. */
    PRIMARY,
    /** 1 set counts as 0.5 sets towards this muscle group volume. */
    SECONDARY
}
